import { LoadingStateDirective } from './loading-state.directive';

describe('LoadingStateDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingStateDirective();
    expect(directive).toBeTruthy();
  });
});
